<?php
  /**
  * Implements hook_theme().
  */
function week_block_theme($variables) {
   $theme['week_block'] = array(
     'variables' => array('nodes' => array()),
     'template' => 'week_block',
   );
   return $theme;
}
